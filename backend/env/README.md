# All about .env

Right now, the only values needed in the `.env` are
`PORT` : ... **backend port**
`FRONTEND_URI` : ... **the frontend url**
`APPWRITE_KEY` : ...
`APPWRITE_ID`  : ...
`APPWRITE_ENPOINT` : ...

If you make any changes, ensure to add it here for others to know :)
